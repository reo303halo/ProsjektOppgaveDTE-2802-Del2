@page "/posts/{BlogId}"

<h3>Posts</h3>

<a class="btn btn-primary" href="post">Create</a>

@if (posts == null) 
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table" >
    <thead>
    <tr>
        <th>Title</th>
        <th>Content</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var post in posts)
    {
        <tr>
            <td>@post.Title</td>
            <td>@post.Content</td>
            <td><a href="comments/@post.PostId">View</a></td>
            <td><a href="post/@post.PostId">Edit</a></td>
            <td><a href="posts" @onclick="@(e =>DeleteBlog(post.PostId))">Delete</a></td>
        </tr>
    }
    </tbody>
    </table>
}

@code {
    [Parameter]
    public string BlogId { get; set; }
    private Post[] posts;

    protected override async Task OnInitializedAsync()
    {
        posts = await HttpClient.GetFromJsonAsync<Post[]>($"/api/Post?blogId={BlogId}");
    }

    private async Task DeleteBlog(int id)
    {
        var response = await HttpClient.DeleteAsync($"/api/Post/{id}");
    }
}
